- if resource.errors.any?
  #error_explanation
    %h2= t('errors.template.header', count: resource.errors.count, model: resource.model_name.human)
    %ul
      - resource.errors.full_messages.each do |msg|
        %li= msg